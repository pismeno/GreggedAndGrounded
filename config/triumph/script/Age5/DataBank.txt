setIcon(<gregtech:machine:1037>)
setTranslatedTitle("Data.")
setTranslatedDescription("You can construct a data bank - it stores large amounts of data (no way). Use this to connect Optical Data Hatches from Data Bank to Assembly lines. This is done with Optical Fiber Cables.")

addParent("Age5:FusionReactor1")

setShowToast(true)
visibleIf("Age5:root")

criteria = addCriteria("ItemInv", "minecraft:inventory_changed")
criteria.addItem(<gregtech:machine:1037>)
setPos(530,99)