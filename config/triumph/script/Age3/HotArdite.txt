setIcon(<gregtech:meta_ingot_hot:24000>)
setTranslatedTitle("Ouch!")
setTranslatedDescription("With Blast Furnace you can also process Ardite, but you also need to cool it down so it becomes usable...")

addParent("Age3:BlastFurnace")

setShowToast(true)
visibleIf("Age3:root")

criteria = addCriteria("ItemInv", "minecraft:inventory_changed")
criteria.addItem(<gregtech:meta_ingot_hot:24000>)
setPos(168,-66)