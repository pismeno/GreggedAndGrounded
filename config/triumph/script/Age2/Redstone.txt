setIcon(<minecraft:redstone>)
setTranslatedTitle("Redstone")
setTranslatedDescription("Break Redstone Ore with Hammer, then put them in crafting table together. To clean the impure dust drop it into a Cauldron with water.")

addParent("Age2:AdvancedProspector")

setShowToast(true)
visibleIf("Age2:root")

criteria = addCriteria("ItemInv", "minecraft:inventory_changed")
criteria.addItem(<minecraft:redstone>)
setPos(96,99)