setIcon(<gregtech:machine:1625>)
setTranslatedTitle("Crates")
setTranslatedDescription("You can now make Crates, they're like chests, but you can open them even if there's block above them.")

addParent("Age2:Steel")

setShowToast(true)
visibleIf("Age2:root")

criteria = addCriteria("ItemInv", "minecraft:inventory_changed")
criteria.addItem(<gregtech:machine:1625>)
setPos(294,0)