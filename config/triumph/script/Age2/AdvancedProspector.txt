setIcon(<prospectors:prospector_high>)
setTranslatedTitle("Advanced Ore Prospecting")
setTranslatedDescription("Craft Advanced Ore Prospector. This one can detect every ore in the game, but only in overworld or the nether.")

addParent("Age2:root")

setShowToast(true)
visibleIf("Age2:root")

criteria = addCriteria("ItemInv", "minecraft:inventory_changed")
criteria.addItem(<prospectors:prospector_high>)
setPos(30,132)