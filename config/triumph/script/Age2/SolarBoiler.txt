setIcon(<gregtech:machine:3>)
setTranslatedTitle("Green Steam")
setTranslatedDescription("Craft Solar Boiler, however, this version is not so effective, so maybe you want to wait for the high Pressure one.")

addParent("Age2:BrickedHull")

setShowToast(true)
visibleIf("Age2:root")

criteria = addCriteria("ItemInv", "minecraft:inventory_changed")
criteria.addItem(<gregtech:machine:3>)
setPos(63,66)