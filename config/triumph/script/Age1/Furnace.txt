setIcon(<minecraft:furnace>)
setTranslatedTitle("Finally? Furnace?")
setTranslatedDescription("Once you unlocked mechanical age you can craft vannila furnace. But maybe consider to still use the grill instead, since it doesnt need any fuel.")

addParent("Age1:root")

setShowToast(true)
visibleIf("Age1:root")

hideLines()

criteria = addCriteria("ItemInv", "minecraft:inventory_changed")
criteria.addItem(<minecraft:furnace>)
setPos(95,-68)