setIcon(<primal:barrel>)
setTranslatedTitle("Storing Water")
setTranslatedDescription("Craft Barrel and use Fluid Bladder to fill it. TIP: you can do shift R-click to carry it.")

addParent("Age0:WorkBlade")

setShowToast(true)
alwaysVisible()

criteria = addCriteria("Barrel", "minecraft:inventory_changed")
criteria.addItem(<primal:barrel>)
setPos(187,-4)