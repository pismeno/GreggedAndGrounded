setIcon(<primal_tech:wooden_basin>)
setTranslatedTitle("Wooden Basin")
setTranslatedDescription("Craft Wooden Basin. NOTE: The items of the recipes have to be put in top down order as shown in JEI.")

addParent("Age0:Grill")

setShowToast(true)
alwaysVisible()

criteria = addCriteria("Basin", "minecraft:inventory_changed")
criteria.addItem(<primal_tech:wooden_basin>)
setPos(253,62)